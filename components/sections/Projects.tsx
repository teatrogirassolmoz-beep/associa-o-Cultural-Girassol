'use client';
import { projects as fallback } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { Button } from '@/components/ui/Button'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { useRows, img, useSection, text } from '@/components/publicCms';
export function Projects(){const f=useSection('home_projects');const rows=useRows('projects',fallback as any[],q=>q.eq('is_active',true).order('featured',{ascending:false}).order('order_index'));return <section id="projectos" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow={text(f.eyebrow,'Projectos')} title={text(f.section_title,'Programas estruturantes')}/><div className="grid gap-6 md:grid-cols-2">{rows.map((p:any)=><Card key={p.id||p.title} className={p.featured||p.link?'md:col-span-2 bg-gradient-to-br from-ember/20 to-sun/10':''}>{img(p.image_url)&&<img src={img(p.image_url)} alt={p.title} className="mb-4 aspect-video w-full rounded-2xl object-cover"/>}<h3 className="text-2xl font-bold text-white">{p.title}</h3><p className="mt-3 text-zinc-300">{p.description}</p>{p.link&&<Button href={p.link} className="mt-6">Abrir projecto</Button>}</Card>)}</div></div></section>}
