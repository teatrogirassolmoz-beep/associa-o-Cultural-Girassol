'use client';
import { Button } from '@/components/ui/Button'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { useSection, text } from '@/components/publicCms';
export function Tickets(){const f=useSection('fiti_tickets');return <section id="bilhetes" className="py-24"><div className="mx-auto max-w-4xl px-4 text-center"><SectionTitle eyebrow={text(f.eyebrow,'Bilhetes e Reservas')} title={text(f.section_title,'Acesso aos espectáculos')}/><p className="text-lg text-zinc-300">{text(f.section_text,'Consulte as informações de acesso aos espectáculos e actividades do FITI.')}</p><div className="mt-8 flex flex-wrap justify-center gap-4"><Button href={text(f.primary_button_link,'#formularios')}>{text(f.primary_button_text,'Reservar lugar')}</Button><Button href={text(f.secondary_button_link,'#contacto')}>{text(f.secondary_button_text,'Falar com a organização')}</Button></div></div></section>}
