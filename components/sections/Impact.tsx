'use client';
import { impactStats } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { useRows, useSection, text } from '@/components/publicCms';
export function Impact(){const f=useSection('home_impact');const rows=useRows('impact_stats',impactStats.map((label)=>({label,value:'',suffix:''})) as any[],q=>q.eq('is_active',true).order('order_index'));return <section id="impacto" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow={text(f.eyebrow,'Impacto')} title={text(f.section_title,'Indicadores institucionais')}/><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{rows.map((s:any)=><Card key={s.id||s.label}><p className="text-3xl font-black text-sun">{s.value}{s.suffix}</p><p className="mt-2 text-lg font-bold text-white">{s.label}</p></Card>)}</div></div></section>}
