'use client';
import { companies } from '@/lib/data'; import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { Button } from '@/components/ui/Button'; import { useRows, img, useSection, text } from '@/components/publicCms';
export function Companies(){const f=useSection('fiti_companies');const rows=useRows('fiti_companies',companies as any[],q=>q.eq('is_active',true).order('order_index'));return <section id="companhias" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow={text(f.eyebrow,'Companhias')} title={text(f.section_title,'Artistas e grupos')}/><div className="grid gap-5 md:grid-cols-3">{rows.map((c:any)=><Card key={c.id||c.name}>{img(c.logo_url||c.image_url)?<img src={img(c.logo_url||c.image_url)} alt={c.name} className="mb-4 aspect-square w-full rounded-3xl object-cover"/>:<div className="mb-4 aspect-square rounded-3xl bg-white/5"/>}<h3 className="text-xl font-bold text-white">{c.name}</h3><p className="text-sun">{c.country}</p><p className="mt-2 text-zinc-400">{c.description}</p><p className="mt-2 text-zinc-300">{c.show||c.show_title}</p><Button href="#programacao" variant="ghost" className="mt-5">Ver programação</Button></Card>)}</div></div></section>}
