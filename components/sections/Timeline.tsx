'use client';
import { Card } from '@/components/ui/Card'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { Sparkles } from 'lucide-react'; import { timeline as fallback } from '@/lib/data'; import { useRows, useSection, text } from '@/components/publicCms';
export function Timeline(){const f=useSection('home_timeline');const rows=useRows('timeline',fallback as any[],q=>q.eq('is_active',true).order('order_index'));return <section id="historia" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow={text(f.eyebrow,'História')} title={text(f.section_title,'Linha do tempo')}/><div className="grid gap-5 md:grid-cols-3">{rows.map((i:any)=><Card key={i.id||i.year}><Sparkles className="mb-4 text-sun"/><p className="text-3xl font-black text-ember">{i.year}</p><h3 className="mt-3 text-xl font-bold text-white">{i.title}</h3><p className="mt-2 text-zinc-400">{i.description}</p></Card>)}</div></div></section>}
