'use client';
import { Card } from '@/components/ui/Card'; import { Button } from '@/components/ui/Button'; import { SectionTitle } from '@/components/ui/SectionTitle'; import { useRows, img, useSection, text } from '@/components/publicCms';
export function Program(){const f=useSection('fiti_program');const rows=useRows('fiti_program',[] as any[],q=>q.eq('is_active',true).order('date').order('time'));return <section id="programacao" className="py-24"><div className="mx-auto max-w-7xl px-4"><SectionTitle eyebrow={text(f.eyebrow,'Programação')} title={text(f.section_title,'Programação FITI')}/>{rows.length===0?<p className="rounded-3xl border border-white/10 bg-zinc-900/60 p-6 text-center text-zinc-300">{text(f.empty_state_text,'Programação em actualização.')}</p>:<div className="grid gap-5 md:grid-cols-2">{rows.map((p:any)=><Card key={p.id||p.title}>{img(p.image_url)&&<img src={img(p.image_url)} alt={p.title} className="mb-4 aspect-video w-full rounded-2xl object-cover"/>}<p className="text-sm uppercase tracking-widest text-sun">{p.date} • {p.time} • {p.venue}</p><h3 className="mt-2 text-2xl font-bold text-white">{p.title}</h3><p className="text-zinc-300">{p.company} {p.country&&`— ${p.country}`}</p><p className="mt-2 text-zinc-400">{p.synopsis}</p><p className="mt-2 text-sm text-zinc-500">{p.category} {p.duration&&`• ${p.duration}`} {p.age_rating&&`• ${p.age_rating}`}</p>{p.reservation_link&&<Button href={p.reservation_link} className="mt-5">Reservar</Button>}</Card>)}</div>}</div></section>}
